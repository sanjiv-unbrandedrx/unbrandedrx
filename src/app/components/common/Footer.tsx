import Logo from "../svg/logo";

export default function Footer() {
  return (
    <footer className="row-start-3 flex gap-[24px] flex-wrap items-center justify-center mt-24 py-8">
      <Logo />
    </footer>
  );
}
